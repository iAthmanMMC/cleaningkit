% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_interview_time.R
\name{validate_interview_time}
\alias{validate_interview_time}
\title{Validate Interview Time}
\usage{
validate_interview_time(
  dataset,
  uuid_column = "_uuid",
  time_column = "start",
  log_name = "interview_time_log",
  earliest_hour = 5,
  latest_hour = 22,
  flag_missing = FALSE,
  skip_label_row = TRUE
)
}
\arguments{
\item{dataset}{A dataframe or a list containing a dataframe named \code{checked_dataset}.}

\item{uuid_column}{The name of the column containing the unique identifier. Default is \code{"_uuid"}.}

\item{time_column}{The name of the column holding the interview start timestamp.
Default is \code{"start"}. Accepts an ISO 8601 character string (as ONA exports it)
or a \code{POSIXct} column (as produced by \code{readxl} when it auto-parses dates).}

\item{log_name}{The name of the log to create in the list. Default is \code{"interview_time_log"}.}

\item{earliest_hour}{Integer 0-23. Interviews starting before this hour are flagged. Default is \code{5}.}

\item{latest_hour}{Integer 0-24. Interviews starting at or after this hour are flagged. Default is \code{22}.}

\item{flag_missing}{Logical. If \code{TRUE}, records whose time is missing or cannot be
parsed are also flagged. Default is \code{FALSE}.}

\item{skip_label_row}{Logical. If \code{TRUE} (the default), the first row of the dataset is removed
before validation. ONA exports include a label/description row immediately after the header
that should not be treated as survey data.}
}
\value{
A list containing the original dataset and the new log dataframe.
}
\description{
Checks that each interview was conducted at a plausible time of day and flags any
records that fall outside an acceptable window (for example, interviews recorded in
the middle of the night), which may suggest the data was fabricated or mis-recorded.
}
\details{
The hour of the interview is taken from the interview start timestamp
(\code{time_column}). ONA exports this column as an ISO 8601 datetime such as
\code{"2023-05-12T09:34:21.000+03:00"}. The local wall-clock hour recorded in the
timestamp is used (the offset the enumerator's device recorded), so no timezone
conversion is applied. A record is flagged when its hour is earlier than
\code{earliest_hour} or at/after \code{latest_hour}.
}
